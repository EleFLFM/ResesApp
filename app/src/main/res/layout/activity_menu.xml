<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/fondo"
    tools:context=".Menu">

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="395dp"
        android:layout_height="305dp"
        android:layout_centerHorizontal="true"
        android:scaleType="fitXY"
        android:src="@drawable/fotor" />

    <Button
        android:id="@+id/machoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/logoImageView"
        android:layout_marginTop="16dp"
        android:drawableLeft="@drawable/calaverap"
        android:drawablePadding="10dp"
        android:padding="12dp"
        android:text="Machos"
        android:onClick="ViewMachos"
        android:textColor="#ffffff"
        android:textSize="18sp" />

    <Button
        android:id="@+id/hembraButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/machoButton"
        android:layout_marginTop="16dp"
        android:drawableLeft="@drawable/calaverap"
        android:drawablePadding="10dp"
        android:padding="12dp"
        android:text="Hembras"
        android:onClick="ViewHembras"
        android:textColor="#ffffff"
        android:textSize="18sp" />

    <Button
        android:id="@+id/todosButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/hembraButton"
        android:layout_marginTop="16dp"
        android:drawableLeft="@drawable/calaverap"
        android:drawablePadding="10dp"
        android:padding="12dp"
        android:text="Todos"
        android:textColor="#ffffff"
        android:onClick="ViewAll"
        android:textSize="18sp" />

    <Button
        android:id="@+id/userButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/todosButton"
        android:layout_marginTop="16dp"
        android:drawableLeft="@drawable/calaverap"
        android:drawablePadding="10dp"
        android:padding="12dp"
        android:text="Actualizar perfil"
        android:textColor="#ffffff"
        android:onClick="ViewActualizarUsuario"
        android:textSize="18sp" />

    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/userButton"
        android:layout_marginTop="16dp"
        android:drawableLeft="@drawable/calaverap"
        android:drawablePadding="10dp"
        android:onClick="ViewLogin"
        android:padding="12dp"
        android:text="Cerrar sesion"
        android:textColor="#ffffff"
        android:textSize="18sp" />

</RelativeLayout>
